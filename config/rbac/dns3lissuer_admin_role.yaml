# This rule is not used by the project dns3l-certmgr-issuer itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over dns3l.github.com.github.com.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: dns3l-certmgr-issuer
    app.kubernetes.io/managed-by: kustomize
  name: dns3lissuer-admin-role
rules:
- apiGroups:
  - dns3l.github.com.github.com
  resources:
  - dns3lissuers
  verbs:
  - '*'
- apiGroups:
  - dns3l.github.com.github.com
  resources:
  - dns3lissuers/status
  verbs:
  - get
