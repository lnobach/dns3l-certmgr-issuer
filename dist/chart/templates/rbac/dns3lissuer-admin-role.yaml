{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "dns3l-certmgr-issuer.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "dns3l-certmgr-issuer.resourceName" (dict "suffix" "dns3lissuer-admin-role" "context" $) }}
rules:
- apiGroups:
  - dns3l.github.com.github.com
  resources:
  - dns3lissuers
  verbs:
  - '*'
- apiGroups:
  - dns3l.github.com.github.com
  resources:
  - dns3lissuers/status
  verbs:
  - get
{{- end }}
